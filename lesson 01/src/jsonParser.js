var util = require('./utils');
var log = util.log;

var parser = (function () {
  var parse = function (insert) {


    return dataCount;
  }

  return {
    parse
  };
})();

module.exports = parser;